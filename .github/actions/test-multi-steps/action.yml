name: TEst action
description: Test Action

inputs:
  run_s2:
    description: Run
    type: boolean
    default: false

runs:
  using: "composite"
  steps:
    - name: step 1
      shell: bash -euxo pipefail {0}
      run: exit 0
    - name: echo
      shell: bash -euxo pipefail {0}
      run: echo ${{inputs.run_s2}}
    - name: step 2
      if: ${{inputs.run_s2}}
      shell: bash -euxo pipefail {0}
      run: exit 1
